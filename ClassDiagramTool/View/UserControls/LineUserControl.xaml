<UserControl x:Class="ClassDiagramTool.View.UserControls.LineUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClassDiagramTool.View.UserControls"
             mc:Ignorable="d">
    <Grid>
        <Line Stroke="Black" X1="{Binding From.P.X}" Y1="{Binding From.P.Y}" X2="{Binding From.P.X}" Y2="{Binding To.P.Y}" RenderOptions.EdgeMode="Aliased"/>
        <Line Stroke="Black" X1="{Binding From.P.X}" Y1="{Binding To  .P.Y}" X2="{Binding To  .P.X}" Y2="{Binding To.P.Y}" RenderOptions.EdgeMode="Aliased"/>
        <!--
        <Line Stroke="Black" X1="{Binding From.P(from).X}" Y1="{Binding From.P(from).Y}" X2="{Binding From.P(from).X}" Y2="{Binding To.P(to).Y}" RenderOptions.EdgeMode="Aliased"/>
        <Line Stroke="Black" X1="{Binding From.P(from).X}" Y1="{Binding To  .P(to  ).Y}" X2="{Binding To  .P(to  ).X}" Y2="{Binding To.P(to).Y}" RenderOptions.EdgeMode="Aliased"/>
        <Line Stroke="Black" X1="{Binding From.CenterX}" Y1="{Binding From.CenterY}" X2="{Binding To.CenterX}" Y2="{Binding To.CenterY}"/>
        <Line Stroke="Black" X1="{Binding From.X}" Y1="{Binding From.Y}" X2="{Binding To.X}" Y2="{Binding To.Y}"/>
        -->
    </Grid>
</UserControl>
